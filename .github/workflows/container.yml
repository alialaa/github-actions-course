name: Container workflow

on: [push]

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container: 
      image: node:13.5.0-alpine3.10
#      env:
#      ports: 
#      volumes:
    steps:
      - name: log node version
        run: | 
          node -v
          cat /etc/os-release
