name: Shell Commands 

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    environment: none
    steps: 
      - name: echo a string
        run: eCCho "Hello Achim"
      - name: multiline script 
        run: |
           node -v 
           npm -v